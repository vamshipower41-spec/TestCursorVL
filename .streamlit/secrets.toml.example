# Paste your daily Upstox access token here.
# On Streamlit Community Cloud, add this in the app's Secrets settings
# (Settings → Secrets → paste the line below).
UPSTOX_ACCESS_TOKEN = "your_daily_access_token_here"
